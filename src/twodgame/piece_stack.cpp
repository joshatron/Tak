PieceStack::PieceStack(vec2 c, double w, double h, int hType, vec3 l, vec3 d, vec3 lh, vec3 dh, string s):
Element(vec2 c, double w, double h, int hType, vec3 l, vec3 d, vec3 lh, vec3 dh)
{
    stack = s;
}

void PieceStack::draw(Screen& game)
{
    
}
